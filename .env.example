# Environment Configuration
ENV=PREPROD

# Dashboard Configuration (optional)
K6_WEB_DASHBOARD_PORT=5665
K6_WEB_DASHBOARD_OPEN=true

# NOTE: Authentication cookies are now generated via "npm run cookies-setup"
# and stored in utils/cookies.json (git-ignored)
# No need to manually set CF_KEY_PAIR_ID, CF_POLICY, etc. anymore!
